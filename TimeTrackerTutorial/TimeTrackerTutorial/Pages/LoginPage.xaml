<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TimeTrackerTutorial.Pages.LoginPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pagemodels="clr-namespace:TimeTrackerTutorial.PageModels"
    x:DataType="pagemodels:LoginPageModel">
    <ContentPage.Content>
        <StackLayout>
            <Label HorizontalOptions="CenterAndExpand" Text="LoginPage" />
            <Entry Placeholder="username" Text="{Binding Username}" />
            <Entry
                IsPassword="True"
                Placeholder="password"
                Text="{Binding Password}" />
            <Button Command="{Binding LogInCommand}" Text="Log In" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>